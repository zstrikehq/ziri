# Backend URL for the LLM Gateway API
NUXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Proxy URL (defaults to http://localhost:3100, use https:// when SSL certs are configured)
# NUXT_PUBLIC_PROXY_URL=https://localhost:3100
